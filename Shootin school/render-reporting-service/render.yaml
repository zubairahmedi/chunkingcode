services:
  - type: web
    name: shootin-school-reporting-service
    runtime: node
    plan: free
    rootDir: Shootin school/render-reporting-service
    buildCommand: npm install && npx playwright install chromium
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_VERSION
        value: 20
